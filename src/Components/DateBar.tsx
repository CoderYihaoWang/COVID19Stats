import React from 'react';


interface IProps {
    setStartDate: (date: Date) => void
    setEndDate: (date: Date) => void
}

export default function DateBar(props: IProps) {

    return (
        <p>placeholder for date bar</p>
    )
}